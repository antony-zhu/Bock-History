# Block tests

本目录预留 Block 的单元、集成、弹性和验收测试。最低门禁包括：

- 无 Wi-Fi、无 BDM 时本地采集、状态、HMI、报警和历史正常；
- 重启后快照、活动报警、报警历史、账号、命令和审计数据可恢复；
- 断网或重连不阻断本地功能；MQTTS v2 仅保留最新当前状态，不补传中间状态；
- 正确证书成功，错误/过期证书、错误主机名、旧 TLS 和明文端口失败；
- 不允许任何测试跳过证书校验。
