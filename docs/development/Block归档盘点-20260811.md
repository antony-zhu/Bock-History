# Block 归档盘点（2026-08-11）

## 目的与范围

本盘点为当前 Block 工作树归档前的无秘密记录。它覆盖 Git 线性范围
`b1b2862321949725fa40596653f38f7fceae10e4` 至
`40bbe8e39c5d5b8c2ec0415fd72a72ea3df14ae2`，以及本次补齐的可复现构建链。
它不连接设备、不访问 PLC、不读取 Wi-Fi、证书、账号、数据库或现场日志，不能替代
受保护的真机发布报告。

## 证据等级

| 等级 | 含义 | 本文用法 |
| --- | --- | --- |
| A：源码与自动化 | Git 提交、已跟踪源码、测试或本次本地命令可直接复核 | 证明实现和本地构建/测试，不证明设备运行。 |
| B：受保护发布记录 | 已有受保护报告或现有发布文档明确记载 | 仅保留其历史结论，不在本次重放。 |
| C：操作员观察 | 本次对话中的操作员描述 | 作为待验证线索，不能升级为产品或真机结论。 |
| 无证据 | 没有制品、部署报告、设备日志或现场屏幕 | 明确标为未验证。 |

## 结论摘要

- `b1b2862..40bbe8e` 是连续的当前源码链；A 级证据表明其中包含 PLC 扫描、点位、
  HMI、报警和刷新修复，以及相应测试增补。
- 最后可从 Git 明确识别的本地 HMI `.10` 候选对应 **`51944eb`**：它把
  `index.html` 的 `hmi.mjs` 查询版本改为 `20260810.10`。`40bbe8e` 改为
  `20260811.1`，因此不对应 `.10` 候选。
- 未找到 `40bbe8e` 的制品 manifest、部署记录、设备版本、屏幕截图或真机验收证据。
  它只能表述为 A 级源码/本地测试修复，不能表述为已部署或已上机。
- 操作员最后一次反馈是“全部报警开启后仍只显示 3 个”，随后确认 PLC 连接有问题且
  已没有设备（C 级）。这意味着 15 报警的真机端到端验证未完成；不能以本地单元测试
  或本次构建替代该验证。
- 原工作树存在把 Go、模块缓存和测试脚本指向历史路径的环境缺陷。本次新增的正式
  构建入口已在全新 state root 验证通过；工具链和缓存不进入 Git。该次构建发生在
  当前未提交工作树，二进制 hash 仅是**预提交验证 hash**，不能作为最终归档 hash。

## Git 范围内的修复盘点

### 1. HMI 视觉底座

| 提交 | A 级可复核内容 | 边界 |
| --- | --- | --- |
| `b1b2862` | 软键盘改为实色渲染，并更新 HMI Node/UI 契约测试。 | 只证明源码和测试；不证明设备屏幕效果。 |
| `fc685ad` | 扩展 `points.json`、HMI bindings、PLC 扫描/worker 和页面，接入主页动作、自动速度、生产数据及目标产能。 | 点位地址/现场动作仍须设备管理员按发布流程确认。 |
| `0ad957c` | 调整节拍显示并移除模式控制的页面入口，测试同步更新。 | 不改变 PLC 联锁或现场模式的真机结论。 |

### 2. PLC 扫描、连接与 SQLite endpoint

| 提交 | A 级可复核内容 | 测试/限制 |
| --- | --- | --- |
| `38e2df5` | `PLCWorker` 改为完整扫描结束后再等待 `500 ms`；成功写、脉冲完成和已尝试的写失败均进行完整确认读，避免 50 ms tick 堆积。 | `plcworker`、runtime config 与 HMI 测试已修改；本次没有对真实 PLC 计时。 |
| `2efaf10` | 新增 SQLite `007_plc_endpoint.sql`；唯一 PLC endpoint 由 `Store.SavePLCEndpoint/LoadPLCEndpoint` 保存，替代 `plc-endpoint.json`，并收紧断线时写入门禁。 | 存储、endpoint、runtime 和外部模拟器测试在提交中可见；未读取真实数据库。 |
| `34ac23b` | 断线后完整 stale 快照发布；下一轮以 `ReconnectInterval = 10s` 重连，成功完整读回调恢复状态与 HMI 更新。 | 有 worker/agent 外部模拟器测试；不等于现场网络或真实 PLC 的重连验证。 |

### 3. Easy521 写入、脉冲和数值字序

| 提交 | A 级可复核内容 | 边界 |
| --- | --- | --- |
| `3c7a371` | 对 Modbus exception `0x01`（Illegal Function）记录 FC22/FC10 不支持：FC22 回退为新鲜 FC03 + FC06 读改写，FC10 回退为逐寄存器 FC06；并补充 client 测试。 | 回退逻辑是源码行为；没有本次真 PLC 写入授权或证据。 |
| `93dca44` | `int32` 与 `float32` 同时支持 `low-high`/`high-low`，默认点表中 DINT/REAL 采用 low-high；读取和写入的 word 组合均更新并有 worker/runtime config 测试。 | 真实现场字序仍需按受控设备验收。 |
| `fc685ad`、`38e2df5` | 主页/生产数据的 FC10 数值写和脉冲项通过 points/bindings 进入现有 worker 确认读路径。 | 只表示应用映射与测试，不表示控制许可或实际动作。 |

### 4. 15 个报警、raw BOOL 和刷新

| 提交 | A 级可复核内容 | 说明 |
| --- | --- | --- |
| `43f3672` | 在默认 `points.json` 增加 15 个报警定义：`D500.0`–`D500.10` 共 11 个，`D502.0`、`.1`、`.11`、`.12` 共 4 个；当前点表合计 42 点、33 个 BOOL（其中 15 个为报警 BOOL）；HMI 从点位构造活动报警并移除页面模式切换/报警确认伪操作。 | 15 是点表定义数量，不是现场已确认数量。 |
| `3da9458` | HMI 曾改为使用 Agent 计算出的 `alarmActive`。 | 随后发现显示链仍不符合实际输入。 |
| `339d029` | HMI 改为直接按报警点的原始 BOOL `value === true` 生成活动报警，不再依赖 `quality` 或 `alarmActive`。 | 这是最终该链上的 raw BOOL 呈现逻辑。 |
| `51944eb` | `index.html` 加载 live alarm refresh 模块并将 HMI cache version 改为 `20260810.10`。 | 这正是最后 `.10` 本地候选的源码身份依据；没有部署证据。 |
| `40bbe8e` | 隐藏或 `inert` 容器内残留焦点不再被当作活动输入；`points.changed` 先推送，SQLite 报警历史写入失败只记日志而不把确认 PLC 读变成 `PLC_READ_FAILED`。测试覆盖输入保护、刷新和存储失败路径。 | 该修复没有制品/部署/真机证据，不能解释为已解决操作员最后看到的 3 条。 |

### 5. Wi-Fi、SSH、模拟器与既有运行边界

- **Wi-Fi（B/C 级历史资料，不是本次验证）**：现有指南记录 Wi-Fi 采用 DHCP、仅可
  由获授权维护流程处理，并禁止读取或记录配置内容。本次没有连接、扫描、读取或写入
  Wi-Fi，因此没有新增 Wi-Fi 成功/失败结论。
- **SSH（A 级脚本变更，未做设备操作）**：`tests/ssh-bootstrap/verify.ps1` 原来硬编码
  `.tools/go1.26.5` 与历史 `GOMODCACHE`；现改为调用统一 bootstrap、显式 state root，并从
  `COMMON_BASELINE` 的 commit 用 `git show` 提取 Common 契约，绝不默默使用 Common 当前 HEAD。
  它保留 Go test/vet、显式 C 编译器前置的 race 和 linux/arm64 静态构建；本次未进行 SSH
  登录、主机 key 或设备访问。
- **模拟器（A 级）**：`tools/plc-simulator/web/app.ts` 是已跟踪源码，`app.js` 是保留的
  浏览器运行时资产。可复现构建会从 TS 5.6.3 重编译到 state root 并逐字节比对；本次
  没有启动 TCP 模拟器或连接真实 PLC。

## 构建环境问题与修复

### 发现的问题

| 问题 | 归档前状态 | 修复 |
| --- | --- | --- |
| Go 来源不确定 | `deploy/block/build.sh` 只调用 PATH 中的 `go`；开发脚本则假定固定 `.tools/go1.26.5`。 | 正式 `build-release.ps1` 下载并校验官方 Go `1.26.5` ZIP，并把已验证 Go 明确传给 Bash；直接调用低层 `build.sh` 必须显式给 `BLOCK_GO_BIN` 与全部受控环境。 |
| 历史模块缓存依赖 | HMI demo 依赖 `.cache/block-v2-runtime-001/gomodcache`，SSH 验证依赖 `.cache/go/gopath/pkg/mod`。 | 模块只下载到本次 state root 的 `GOMODCACHE`，逐 module 执行 `go mod verify` 后 `GOPROXY=off`。 |
| TypeScript 不可审计 | 没有根 `package.json/package-lock` 或 HMI tsconfig，运行时 JS 是否由确定版本编译无法复核。 | 新增根 lock（精确 `typescript: 5.6.3`）和 `apps/block-hmi/tsconfig.json`；两个 TS 输出均做字节级守卫。 |
| 工具环境未封闭 | 临时目录、缓存和交叉编译标志由调用者手工设置，容易混入用户环境。 | bootstrap/state root 统一设置 `TEMP`、`TMP`、`TMPDIR`、`GOTMPDIR`、`GOPATH`、`GOCACHE`、`GOMODCACHE`，固定 `GOENV=off`、`GOWORK=off`、`GO111MODULE=on`、空 `GOROOT`、`GOARM64=v8.0`，清除私有/不安全 Go、Node/npm 与所有继承 `GIT_CONFIG_*` 注入变量；保留 `HTTP(S)_PROXY` 和正常 Git 代理仅作传输路径。release 强制 Linux/arm64、`-mod=readonly`、`-trimpath`、`-buildvcs=false`。 |
| VCS 与缓存身份不明确 | 脏工作树、用户 Go env 或无 owner 的非空缓存可能产生不可复核 hash，或误删外部目录。 | 正式 release 默认拒绝 dirty/untracked 工作树，制品记录 commit/tree/clean 状态；未传 `-StateRoot` 时按版本使用独立 `.cache/block-release-<version>`，而 bootstrap 独立调用才使用 `.cache/block-build`。state root 仅允许仓库根目录 `.cache` 的直接子目录，带 owner marker，拒绝仓库根、卷根、junction 和非拥有的非空目录；空无 marker 目录可安全初始化，`-FreshState` 只删除已有 owner marker 的非空目录。 |
| 生成物回流风险 | 根 ignore 未明确覆盖根/嵌套 `.cache`、`node_modules`、`target` 等。 | `.gitignore` 明确忽略这些生成目录，保留源码、脚本、锁文件和已跟踪运行时资产。 |
| Git Bash 回归夹具的链接降级 | 当前 Windows Git Bash 在默认设置下会把 `ln -s <目录>` 降级为普通目录；回滚测试中的 `current` 不再是链接，`readlink -f` 与 `-L` 断言失真。 | 两个部署回归脚本仅在 `MINGW/MSYS/CYGWIN` 测试环境设置 `MSYS=winsymlinks:native`；Windows 测试 mock 还跳过目录 chmod。真实 Linux 安装/回滚脚本没有改为 Windows 语义。 |
| Git Bash 安装位置假定 | release 原先只尝试固定 `C:\Program Files\Git`，非标准 Git for Windows 安装会失败或误用 WSL/系统 `bash.exe`。 | release 先从 PATH 的 `git.exe` 推导 Git for Windows 根目录，也接受显式 `-GitBash`/`BLOCK_GIT_BASH`；拒绝 WSL/System32 bash，并要求同一安装内可执行的 `cygpath.exe`。 |
| 默认点表回归误判 | 原部署回归用 `float32|fc10` 泛匹配排除模拟器点；但正式默认点表合法包含 `D522` 的 FLOAT32 读点和 `D1000` 的 FC10 DINT 写点。 | 改为逐个排除 legacy `simulatorFloat32` 的 D800–D852 专用地址，保留对正式数值点的验证，不改变任何 PLC 点位或运行逻辑。 |

### 当前正式入口

```powershell
.\tools\build-release.ps1 -Version <approved-unique-version> `
  -StateRoot '.cache\<one-clean-build>'
```

入口下载工具链、锁定 TS、下载/验证 Go 模块、离线测试与 Linux ARM64 构建。正式构建要求
干净 Git 工作树，并把 commit/tree/clean 状态写入 `artifact/build-metadata.json`；`-AllowDirty`
只可用于开发诊断。`deploy/block/build.sh` 会拒绝缺失或不符合要求的 `GOENV`、`GOWORK`、
`GO111MODULE=on`、空 `GOROOT`、`GOTOOLCHAIN`、`CGO_ENABLED`、`GOOS`、`GOARCH`、`GOARM64=v8.0`、离线校验环境、缓存目录或
Go `1.26.5`。HMI demo 和 SSH 验证都通过同一 bootstrap 获得工具链，而不是引用历史路径。

## 本次全新 state root 验证（A 级）

执行日期：2026-08-11；开始前下列 state root 不存在：

```text
D:\codex\Block-DMP\.cache\archive-repro-build-20260811
```

执行命令：

```powershell
.\tools\build-release.ps1 -Version archive-repro-20260811.1 `
  -StateRoot '.cache\archive-repro-build-20260811'
```

结果：

| 项目 | 结果 |
| --- | --- |
| 官方 Go ZIP | `go1.26.5.windows-amd64.zip`，固定 SHA-256 `97e6b2a833b6d89f9ff17d25419ac0a7e3b482a044e9ab18cdef834bd834fd38`；实际 `go version go1.26.5 windows/amd64`。 |
| 官方 Node ZIP | `node-v24.14.0-win-x64.zip`，固定 SHA-256 `313fa40c0d7b18575821de8cb17483031fe07d95de5994f6f435f3b345f85c66`；实际 `v24.14.0`。 |
| 锁定 TypeScript | `npm ci` 仅从根 lock 安装；实际 `Version 5.6.3`。 |
| 运行时资产 | HMI `hmi.mjs` SHA-256 `679b52854b08eb46ccf14448999f2048199c57ed778f605003ec1139dc6ab710`；模拟器 `app.js` SHA-256 `4e4940bfcaa60a55a7dff183ffcb8a72c5aeb43853c07ebf16b13796f0e277b2`。两者由 state root 重编译并逐字节一致。 |
| Go 依赖 | `apps/block-hmi`、`services/block-agent`、`tools/plc-simulator` 均执行 `go mod download` 与 `go mod verify`；模块只落在该 state root。 |
| 测试 | HMI Node 测试、三个 Go module 的 `go test -mod=readonly ./...` 和 Agent `go vet -mod=readonly ./...` 均通过。 |
| Release | `GOPROXY=off` 下生成 AArch64 ELF；`artifact/bin/block-agent` SHA-256 为 `a5c1672ff89c24a436a3f91c1899ca87dfc9a9e3fcb33840a46c3acc05c4b4c6`。这是**预提交验证 hash**，不是最终归档 hash；提交并清理工作树后必须以正式入口重新构建并记录最终 hash。 |

首次直接访问 `proxy.golang.org` 时发生 TLS 连接超时；这暴露了原来只依赖单一代理的
环境风险。正式默认下载链改为可配置的
`https://goproxy.cn|https://proxy.golang.org|direct`，其中 `|` 允许网络错误时 fallback，
但不声称镜像永远可用；仍保留 `sum.golang.org` 与 `go.mod/go.sum` 校验，随后才离线构建。
上述全新 state root 构建只访问了工具/模块下载网络，未访问设备、PLC 或现场网络；没有读取、
复制或引用任何历史 `GOMODCACHE`。

## 本轮增量脚本与回归验证（A 级）

本节验证的是本轮尚未提交工作树中的脚本和回归修正，不替代上节的全新可复现 release，也不能
产生最终归档 hash。

| 项目 | 实际结果 | 证据边界 |
| --- | --- | --- |
| 脚本与文本检查 | `tools/build-state.ps1`、`bootstrap-build-tools.ps1`、`build-release.ps1`、`test-build-release-state-root.ps1`、`cleanup-build-artifacts.ps1`、`start-block-hmi-auth-demo.ps1`、`tests/ssh-bootstrap/verify.ps1` 均通过 PowerShell parser；state-root 测试验证版本隔离、任意 `GIT_CONFIG_*` 清除与 `HTTP(S)_PROXY` 保留；受改动的 Bash 脚本通过 `bash -n`；`package.json` 可解析，`git diff --check` 通过。 | 只证明语法、环境策略和文本一致性。 |
| 部署回归 | `deploy/block/verify-static.sh`、`install-rollback-regression.sh` 与完整 `deploy-regression.sh` 均通过。后者覆盖默认/模拟 PLC profile、安装预检、TLS 缺失拒绝、安装失败自动回滚、成功切换和手工回滚。 | 使用 Git Bash 本地夹具；没有安装到设备。 |
| HMI | `node --test assets/hmi.test.mjs` 通过。 | 无浏览器真机画面或设备连接。 |
| Go | 使用已校验的 Go `1.26.5`，`GOPROXY=off`、`GOSUMDB=off`、`-mod=readonly`，三个模块的 `go test ./...` 均通过。 | 为避免重跑下载式 release，本项以旧验证目录中的模块缓存作为离线输入；这只是当前源码的离线测试，不是新 bootstrap 的全新下载证明，也不构成正式 release。 |
| SSH 验证入口 | 当前 `verify.ps1` 已通过 parser；完整入口本轮未运行。 | 完整入口会创建全新受管 state root 并下载/校验工具及模块，race 门禁还要求本地 `gcc` 或 `clang`；未在本轮重跑下载式验证。 |

本轮 Go 测试产生的受管目录为
`D:\codex\Block-DMP\.cache\archive-validation-20260811`，清理前为 2,738 个文件、
176,663,479 bytes。按 `cleanup-build-artifacts.ps1` 审计清单只删除了该目录的
`gopath/`、`gocache/`、`gotmp/` 和 `tmp/`；现在仅保留 111-byte owner marker。没有删除
源码、配置、设备资料或下文所列旧验证目录；之后未再执行删除操作。

## 清理、保留与秘密边界

| 类别 | 处理 |
| --- | --- |
| 可删除的受管生成物 | 新格式 owner marker state root 下 `downloads/`、`toolchains/`、`gopath/`、`gomodcache/`、`gocache/`、`gotmp/`、`tmp/`、`npm-cache/`、`npm-userconfig`、`npm-globalconfig`、`node-deps/`、`typescript/`、完整 `artifact/`、验证 `bin/`、`artifact.tar.gz` 与 `artifact.sha256`。`tools/cleanup-build-artifacts.ps1` 默认 dry-run，只有用户终端显式 `-Execute` 才删这些精确目标；不删源码、README、配置、SSH 材料或证据。 |
| 可删除的一般生成物 | 根/嵌套 `.cache`、`node_modules`、`dist`、`bin`、`out`、`coverage`、`target`、临时日志和测试二进制；删除前必须核对是生成物而非源码或设备资料。 |
| 必须保留 | Git 跟踪的源码、`go.mod`/`go.sum`、`package.json`/`package-lock.json`、tsconfig、部署脚本、点表、测试、文档，以及已跟踪的 `apps/block-hmi/assets/hmi.mjs`、`tools/plc-simulator/web/app.js`。后两者在本次保留为运行时资产，并由字节比对守卫。 |
| 不进入归档或 Git | 真实 `wifi.toml`、`.env`、密码、token、私钥、证书私钥、真实数据库、现场日志、设备二进制、SSH 身份和可复用连接资料。只记录存在性或无秘密哈希时也须先获得相应授权。 |

本次验证 state root `D:\codex\Block-DMP\.cache\archive-repro-build-20260811` 目前仍存在，
为 25,838 个文件、1,199,031,648 bytes（约 1.12 GiB）。它是 owner marker 规则引入前的
预提交验证目录；自动清理在本会话被执行环境策略拒绝，且不得通过绕过策略或把它 retroactively
标记为新工具拥有。**尚未物理删除**；必须由用户在本机终端按已审计的精确路径另行处理。

## 未完成项

1. 没有设备，因此未验证 `40bbe8e` 在真实 HMI 上由 3 条切换到 15 条报警的画面、PLC
   连接、500 ms 扫描、10 秒重连、写入回读或异常回退。
2. 没有新的候选制品部署、SSH 会话、Wi-Fi 操作、数据库迁移或真机屏幕证据；这些均需
   新的 Release 授权并遵循 `deploy/block/README.md`。
3. `hmi.mjs` 与 `app.js` 保留为已跟踪运行时资产；若今后源码改变，应通过正式入口先
   产生字节差异，再审查并有意更新资产，不能让构建静默覆写它们。
4. 本次 `a5c1672f…` 仅为脏工作树下的预提交验证 hash。完成提交后，必须使用一个新的、
   空的 owner marker state root 运行不带 `-AllowDirty` 的正式构建，才可记录最终归档 hash。
