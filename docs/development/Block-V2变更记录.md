# Block V2 变更记录

本文件是 Block V2 的唯一持续变更记录。它只记录已能从 Git、受保护的发布
报告或现有文档核对的事实，用于判断当前状态和下一步；不替代公共架构与契约，
也不复制真机发布操作。真机操作只遵循
[Block v2 真机发布与回滚](../../deploy/block/README.md)。

## 当前状态（截至 2026-08-07）

| 项目 | 已核对状态 |
| --- | --- |
| 当前实现基准 | `c47115b`（`fix(block-hmi): require WSS for local runtime`）与其前置 `9657e1b`（`feat(block-agent): serve local HMI only over TLS`）构成本轮本机 HMI TLS 行为基准，包含 `7621280` 的真实认证持久化演示、`0511847` 的认证字段长度约束和 `5f83b61` 的过期会话保护；共享契约基线为 Common `d1073038277db0b954c021cb2cc377012ec8a78c`，已由 `COMMON_BASELINE` 固定。文档自身的提交不作为功能实现基准。 |
| 真机实际运行版本 | `0.0.0-18c0a5260c50`。受保护部署报告确认，`456b61a69c95b35aec70935c605a1635f1ec3984` 的两次受控安装均在门禁误判后自动回滚；新 release 仅以非活动版本保留。 |
| 本地演示 | `/?demo=1` 及其认证变体进入固定 `1920×1080` iframe 壳；小窗等比缩放和居中。缩放态仍映射到同源 `__demoFrame=1` 的真实 DOM，认证、权限和软键盘逻辑不分叉。演示与运行时均使用严格 CA 校验的 loopback HTTPS/WSS，不存在 `ws://` 或 8080/8081 兼容监听。认证请求同机 Agent 的 v2 无状态 API 和测试 SQLite；真实 `cmd/block-agent` 的本机启动及独立数据库重启持久化脚本已通过。当前 Browser 运行环境没有可用实例，未执行真实点击、截图或矩形验收。该说明不等同于真机验收。 |
| 当前已完成 | 当前功能基准已包含 2026-08-05 至 2026-08-07 的 V2 HMI、模拟器、认证/访客、维护页、发布文档、视觉性能精简、固定工业 viewport 演示及缩放态输入映射；本轮完成 Common v2 无状态认证基线、字段长度约束、过期会话不可续期保护、真实 Agent 持久化验证，以及 `127.0.0.1:8444` 本地 HTTPS/WSS 收口，详见下方时间线。 |
| 尚未发布 | `c47115b`、`9657e1b` 及其前置提交没有真机发布证据；当前设备未运行该候选。未连接设备、未部署或烧写，任何设备写入均需要新的 Release 授权。 |
| 需特别区分 | 2026-08-02 至 2026-08-03 的 PLC 点位、FC22 Mask Write、早期 V2 Demo 和 typed bindings 提交仍可从 Git 历史读取，但不是上述功能基准的祖先；不能把它们描述为当前源码或真机已运行功能。 |
| 下一步 | 1. 获得 Release 授权后，先在设备执行只读预检，记录当前服务、端口、配置摘要、8444 TLS 证书 SAN/CA 和数据备份位置。2. 完成数据备份后，严格按[唯一发布流程](../../deploy/block/README.md)部署本轮候选，执行严格 CA 健康检查、版本记录和回滚准备。3. 在受控设备上完成真实 `1920×1080` HTTPS/WSS HMI 验收并记录结果；现有 MutationObserver 问题不属于本变更，尚未修复。 |

### 记录口径

- **当前功能基准**：用户可见功能以顶部列出的实现提交为准；文档提交不改变该基准。
- **历史分支**：提交在本仓库 Git 引用中存在，但不是顶部功能基准的祖先；它保留
  开发成果和待整合事项，不表示已经合入、已发布或已上机。
- **真机结果**：只以受保护的 `device-deploy-456b61a/deployment-report.md` 为准；
  其余没有报告支撑的内容一律标为本地或待确认。

## 时间线

### 2026-08-02

#### V2 模块化、PLC 只读采集与匿名查看（历史分支）

- **用户目标/决策**：将旧可靠上行替换为 MQTT v2，并把 HMI 收敛到本地 API
  边界，同时允许匿名只读监看。
- **实现结果**：拆出 SQLite 生命周期；建立 PLC 类型、Easy521 读取与点位表核心；
  读取经 PLC 层进入运行时，并补充映射版本边界与原子提交处理。
- **对应提交**：`77d4aee`、`34a2415`、`8a8acee`、`64e7820`、`d1a92a8`、
  `a72fa57`、`a7239da`、`f6c4172`、`7c599a3`、`4e9157a`。
- **验证/真机结果**：相关提交新增 Go 单元测试；未找到该分支的真机发布报告。
- **遗留事项**：上述提交不是 `6393448` 的祖先，必须先受控整合并重做验证，
  不能直接作为当前 V2 功能或真机能力引用。

#### PLC 点位契约与 FC22 Mask Write（历史分支）

- **用户目标/决策**：以可编译的 V2 点位表约束 PLC 映射，并为脉冲操作提供受控
  的 Easy521 FC22 Mask Write 执行路径。
- **实现结果**：加入点位表编译器、证据校验、FC22 脉冲执行器、寄存器 fake 和
  `pulse100` 闭环测试。
- **对应提交**：`f17e960`、`1cec073`、`2938c56`、`f25eac9`、`e5c7e39`。
- **验证/真机结果**：提交中包含编译器、执行器和闭环测试；没有当前 HEAD 或真机
  验收记录。
- **遗留事项**：先确认与当前 Agent 的接口和安全边界，再选择合并/移植方案；
  不得在未整合前对 PLC 写入做任何真机声明。

#### 模拟 PLC、Local API Demo 与受控 sidecar（历史分支）

- **用户目标/决策**：让 V2 点位快照可由模拟器驱动、经本地 API 展示，并提供隔离
  的演示发布路径。
- **实现结果**：新增只读 `plcmap v2` Demo runtime、bindings Local API、客户端
  演示工具，以及隔离 sidecar 的发布/审批修复。
- **对应提交**：`0933685`、`217db67`、`58d9069`、`6ab25ad`、`a37e1e5`、
  `3d0ed74`、`694b1e7`、`47b1532`。
- **验证/真机结果**：提交中带有 demo runtime、bindings 和部署回归测试；没有真机
  部署报告。
- **遗留事项**：该演示链同样不在当前 HEAD 的祖先链上，后续整合前需保留其只读
  性质和发布隔离边界。

### 2026-08-03

#### 模拟器 bindings 与首次 typed TypeScript HMI（历史分支）

- **用户目标/决策**：把模拟 PLC bindings 接到 HMI，并以 TypeScript 提供类型化的
  快照视图。
- **实现结果**：加入 simulator-backed V2 bindings demo 和 typed PLC binding
  snapshot 页面。
- **对应提交**：`7ebc909`、`cebc9e8`。
- **验证/真机结果**：提交新增 bindings 行为/UI 契约测试；没有设备发布证据。
- **遗留事项**：两项提交不是当前功能基准的祖先。当前源码中的 TypeScript HMI 以
  2026-08-05 的提交为准。

#### 登录、首次管理员与会话投影探索（历史分支）

- **用户目标/决策**：明确首次管理员创建、登录状态、会话超时和 HMI 显示之间的
  行为。
- **实现结果**：补充认证/SSH 访问层说明、登录页状态修复以及由 Agent 投影的可配
  置空闲会话。
- **对应提交**：`2ee941b`、`9e16d33`、`608b812`、`4b83881`、`d6dd724`、
  `9aa6924`、`283770e`。
- **验证/真机结果**：提交包含认证、会话和 HMI 测试；没有设备发布证据。
- **遗留事项**：这些历史分支成果不自动等于当前认证实现，后续修改须以当前
  Agent/HMI 实际接口为准。

### 2026-08-05

#### 当前 V2 runtime、单服务部署与本地认证

- **用户目标/决策**：形成当前分支可运行的 Block V2 最小闭环：本地 runtime、
  PLC 会话、HMI 认证、可选 MQTT v2、报警历史和单服务部署。
- **实现结果**：加入会话点位配置、loopback HMI runtime、Easy521 worker、首次
  本地 HMI 认证、PLC 连接/扫描、维护 SSH key endpoint、MQTT v2 与报警历史；
  随后接入 runtime、退役旧 Go HMI 服务，并补齐部署参数、诊断和本地 E2E/扫描
  观察工具。
- **对应提交**：`0d458a6`、`03d0701`、`6f39af8`、`91c7b78`、`d7b97a6`、
  `c218915`、`f10f838`、`3d5ef37`、`711766e`、`11ea8b2`、`9631ac9`、
  `61759b7`、`295f1ac`、`113867b`、`1f7e12b`、`aea33bb`、`99985f1`、
  `cf64e73`、`0af4ffd`、`e46c6d4`。
- **验证/真机结果**：提交加入 Agent、部署与本地 HMI E2E 测试；没有针对这组
  源码的成功真机发布报告。
- **遗留事项**：真实 PLC 适配、现场点位和受控上机仍需单独确认；发布前必须完成
  无 BDM/无 Wi-Fi 的回归验证。

#### Windows PLC 模拟器与当前 TypeScript HMI

- **用户目标/决策**：为电脑侧联调提供 Modbus TCP 模拟 PLC，并让 HMI 由
  `points.json` 与 TypeScript runtime 驱动。
- **实现结果**：新增 PC Modbus TCP 模拟器；重构 HMI 为 points-driven
  TypeScript runtime，并加入 PLC 会话控制；随后给模拟器增加本机管理页。
- **对应提交**：`e5a6d76`、`919c4da`、`8bcc902`、`0f091bc`。
- **验证/真机结果**：提交包含模拟器服务测试、HMI JavaScript 测试和 UI 契约测试；
  无同日设备发布报告。
- **遗留事项**：模拟器仅用于本机/开发联调，不能替代真实 PLC 点位、联锁和真机
  验收。

### 2026-08-06

#### Apple 风格界面、启动命令反馈与软键盘

- **用户目标/决策**：恢复 Apple Style V1 视觉表现，同时保留触摸场景可用的认证
  与软键盘输入，并在启动 PLC 时等待实际命令结果。
- **实现结果**：恢复 Apple 风格界面和软键盘资源；修复 Start PLC 结果等待；
  加入浮动认证输入、认证切换时关闭键盘、键盘避开认证面板及可靠打开键盘的修复。
- **对应提交**：`a358bd2`、`d35f0c5`、`0d2ea21`、`a80e034`、`1141594`、
  `1639911`。
- **验证/真机结果**：提交包含 HMI 行为、布局探针和 UI 契约测试；最终设备截图
  仅证明回滚后的旧版本显示 Apple 风格 HMI，不证明这组提交已发布。
- **遗留事项**：后续发布必须在真实 `1920×1080` 屏幕上重新验收软键盘、认证和
  首屏，不能只以浏览器响应判断。

#### 首次管理员、认证状态与访客权限

- **用户目标/决策**：将首次管理员创建与登录选择由本地认证状态驱动；访客可阅读，
  维护和现场操作保留登录门禁，且访客导航可达。
- **实现结果**：新增认证状态 endpoint；HMI 根据状态选择 bootstrap/login；
  演示入口自动切换认证页；加入本地 HMI 访客权限和访客导航修复。
- **对应提交**：`15ed025`、`51fdd5d`、`cc73e24`、`faea4d6`、`7ebafab`。
- **验证/真机结果**：提交包含认证 HTTP 与 HMI/UI 契约测试；没有该功能的独立
  设备发布报告。
- **遗留事项**：前端交互门禁不能替代 Agent 对本地操作和 PLC 写入的校验；真机
  发布前应再次核对两侧接口和权限语义。

#### 本地维护页恢复

- **用户目标/决策**：恢复本地 HMI 的生产参数、Wi-Fi、PLC 通信和账号管理四个
  维护 tab，并保留触摸输入体验。
- **实现结果**：恢复四个维护 tab；新增生产参数维护接口、本机 Wi-Fi 管理与测试，
  并把维护运行时测试 URL 调整为 HTTP。
- **对应提交**：`60f3b3d`、`18c0a52`。
- **验证/真机结果**：提交包含维护 HTTP、生产参数和 Wi-Fi 单元测试；设备当前
  正在运行的版本正是 `0.0.0-18c0a5260c50`，但报告只验证回滚后的整体健康、
  kiosk 与屏幕，未逐项完成维护 tab 真机验收。
- **遗留事项**：维护功能涉及现场网络和凭据时，仍不得读取、写入或在报告中输出
  `wifi.toml` 内容；真机功能验收应由 Release 角色单独授权执行。

### 2026-08-07

#### 本地认证 v2：SQLite 无状态认证与页面空闲计时

- **用户目标/决策**：账号和空闲时长必须由 Block SQLite 管理；认证不得再依赖
  浏览器存储、SHA-256 摘要、Cookie、Token 或后端登录会话。保留既有认证弹层、
  键盘与访客交互，角色只作为 HMI 前端门禁。
- **实现结果**：`05c035d` 删除 Agent 内存 session、Cookie、活动续期和 logout
  路由。`GET`/`POST /api/v2/auth/initial-admin`、`POST /api/v2/auth/login`、
  `POST /api/v2/auth/password`、`GET`/`PUT /api/v2/config/session` 只使用 SQLite
  账号、Argon2id 哈希和持久化空闲时长。`be2e525` 让 HMI 启动时读取同机 API，
  只在页面内存保存身份和权限，并以 pointer/touch/keydown 重置本地计时器；demo
  也使用同一 API 和测试 SQLite。`/auth/activity`、`/auth/logout` 已退役。
- **验证/真机结果**：TypeScript 编译、`node assets/hmi.test.mjs`、`go test .`
  （HMI UI 契约）和 `git diff --check` 通过。后端定向
  `go test ./internal/auth ./internal/agent ./cmd/block-e2e` 已以工作区 `.cache/**`
  作为全部临时与 Go 缓存目录尝试；`proxy.golang.org` 下载锁定的
  `golang.org/x/crypto v0.52.0` 与 `golang.org/x/net v0.55.0` 时 HTTPS 连接超时，
  未进入编译或测试；未改动 `go.mod`/`go.sum`，未重复重试。未连接设备、未构建
  发布、未部署或烧写。
- **遗留事项**：网络可用时重跑后端定向测试；在可用 Browser 与真实
  `1920×1080` 屏幕中补验登录、首次创建、空闲退出、密码修改和键盘首帧。任何
  真机发布仍须由 Release 角色获得新的设备写入授权后执行。

#### 发布激活、Chromium 缓存验收与唯一发布文档

- **用户目标/决策**：使 release 激活明确重启 Block/kiosk，并把版本、缓存和真实
  屏幕验收写入唯一的受控发布流程。
- **实现结果**：安装器改为在激活时重启 Block 服务；
  [发布文档](../../deploy/block/README.md)补充版本校验、`no-store` 解析、
  `/index.html` 规范化 `301`、Chromium 缓存和 X11 真实屏幕验收规则。
- **对应提交**：`456b61a`、`8b2767b`、`261d2ab`、`0fbdc77`。
- **验证/真机结果**：受保护部署报告确认，目标 `456b61a` 的本地构建、测试和
  制品校验通过；两次安装均通过健康检查和 kiosk 启动，随后分别因
  `Cache-Control` 比较方式与 `/index.html` 的规范化 `301` 被门禁误判，均已
  自动回滚。最终设备保持 `0.0.0-18c0a5260c50`。
- **遗留事项**：下一次发布前先按唯一发布文档核对门禁逻辑，再取得新的设备写入
  授权；不得对同一问题自行连续重试或手工绕过回滚。

#### HMI 视觉性能精简

- **用户目标/决策**：删除对工控机渲染负担较大的 HMI 视觉特效，同时保留界面
  主功能和交互反馈。
- **实现结果**：去除大面积模糊/毛玻璃、扫描叠层、状态脉冲、机器图滤镜与
  指针 3D 跟随等效果；软键盘面板改为不透明背景。
- **对应提交**：`6393448`。
- **验证/真机结果**：该提交是当前功能基准的祖先；没有该提交的真机发布记录。
- **遗留事项**：先执行本地回归，再在获得新授权的发布中验收实际屏幕刷新、
  软键盘和关键按钮反馈。

#### 固定工控机 viewport 的本地演示

- **用户目标/决策**：用户确认本地演示严格按工控机 `1920×1080` 分辨率呈现；
  普通电脑窗口不足时以固定画布等比缩小和居中，不进行内部响应式重排。
- **实现结果**：原 `/?demo=1` 及 `auth` 变体透明跳转到固定 iframe 演示壳；
  iframe 内始终使用 `1920×1080` viewport，外层比例限制为不大于 `1`，非 demo
  页面不进入该壳。
- **对应提交**：`110474c`。
- **验证/真机结果**：浏览器烟测确认 `1600×900` 比例为 `0.83333`、
  `1920×1080` 比例为 `1`，bootstrap/login/软键盘均在画布内；Node 验证通过，
  Go 当时不可用。未进行真机发布。
- **遗留事项**：现有 MutationObserver 错误与本变更无关，尚未修复；下次可用
  Go 工具链时补跑相应验证，并在受控发布时完成真实屏幕验收。

#### 缩放演示壳的登录与输入映射修复

- **用户目标/决策**：在用户明确切换到设备测试前，所有验证仅使用网页版；普通
  `?demo=1` 始终先进入完整访客界面，只有点击“登录”或受限功能后才按本机管理员
  是否存在显示创建管理员或登录页。
- **实现结果**：定位到缩放后的 iframe 在浏览器中无法把真实点击正确交给内部
  HMI；演示壳改为由固定 `1920×1080` wrapper 承载 iframe。仅在缩放态使用透明
  demo 输入层把坐标发给同源 `__demoFrame=1`，内部再激活实际 DOM 目标；不复制或
  分叉认证、权限、软键盘或任何业务逻辑。普通非 demo 路径不注册该接收逻辑。
- **对应提交**：`1a17033`。
- **验证/真机结果**：Browser 外层演示实测：已有管理员访客点击登录显示普通登录并
  打开软键盘；隔离 origin 的无管理员访客显示创建管理员；未登录点击维护同样显示
  登录；软键盘 `q` 可实际输入。非 demo 页面保持访客首屏并在点击登录后显示普通
  登录。Node `assets/hmi.test.mjs` 通过；Go 工具链不可用，未运行 UI 契约测试。
  当前 Browser 环境的 viewport override 未实际改变 `1280×720` 视口，故本次未将
  `1600×900` 与 `1920×1080` 重复标为浏览器实测。未进行真机连接、构建、发布或
  烧写。
- **遗留事项**：需在可实际调整视口且获得相应测试授权的浏览器中补做两种分辨率
  烟测；MutationObserver 控制台错误没有中断认证初始化且不属于本次根因，未修改。

#### 本地认证弹层与软键盘重构

- **用户目标/决策**：移除软键盘顶部输入描述，提供布局合理的完整数字行；把普通
  登录和首次管理员创建统一为更大的两列输入行视觉系统；登录应可通过公共页面导航
  静默退出，且软键盘“完成”与认证按钮使用相同提交语义。
- **实现结果**：普通登录保留“用户名：输入框”和“密码：输入框”两行与密码可见性
  按钮；首次创建管理员采用同一布局并保留三项输入。键盘新增 `1` 至 `0`、`-`、`=`
  顶行，移除正在输入/字段/等待输入元数据。空提交关闭认证并显示“登录失败”，错误
  凭据关闭认证并显示“用户名或密码错误”，成功登录显示“登录成功”；主页、生产数据、
  报警和历史记录导航静默恢复访客状态。
- **对应提交**：`811ba14`。
- **验证/真机结果**：Browser 在 `1920×1080` 画布验证空输入、错误凭据、成功登录、
  创建管理员、密码显示切换与公共导航；`1280×720` 外窗下 iframe 为 `1280×720`，内部
  认证卡和键盘仍完全位于 `1920×1080` 画布内。Node `assets/hmi.test.mjs` 通过；
  Go 工具链不可用，未运行 UI 契约测试。未进行真机连接、构建、发布或烧写。
- **遗留事项**：真机发布前仍需由 Release 角色在真实 `1920×1080` 屏幕验收认证、
  软键盘和关键导航；本变更不修改既有 MutationObserver 问题。

#### 审查修正：认证弹层公共导航撤销

- **用户目标/决策**：审查发现已有恢复会话时，强制打开认证弹层后点击公共页面只会
  隐藏弹层而未清除会话；公共导航撤销认证必须回到访客，但正常已登录浏览公共页面
  不得被无故登出。
- **实现结果**：公共导航监听器仅在认证弹层可见时调用既有 `becomeGuest()` 路径，
  清除会话并关闭认证/软键盘；认证弹层已隐藏时监听器不做状态修改。
- **对应提交**：`110786f`。
- **验证/真机结果**：TypeScript 编译通过；Node `assets/hmi.test.mjs` 断言认证弹层
  可见时调用 `becomeGuest()`，并断言无认证弹层没有替代分支；未进行真机连接、构建、
  发布或烧写。
- **遗留事项**：仍需在受控发布中由 Release 角色完成真实屏幕验收。

#### 认证层、底栏与软键盘视觉精简

- **用户目标/决策**：在不改变认证、访客权限、公共导航撤销、固定画布或软键盘业务语义的前提下，收窄并加高认证 sheet；底栏只保留必要中文；以 Shift/退格图标替换重复“大写”，并取消会挤压认证键盘布局的红色行内提示。
- **实现结果**：登录和首次管理员 sheet 统一为更窄、更多纵向留白的布局，输入框与提交按钮保持原有触控高度；访客身份只显示“登录”，导航及自动状态删除英文和“远程联机”。全键盘数字顶行末键改为 `.`，左侧 Shift 与右侧退格使用带中文 `aria-label` 的内联 SVG；认证字段跳过键盘行内校验，登录和 bootstrap 结果改由既有 toast 反馈，维护表单校验不受影响。
- **对应提交**：`206a8e7`。
- **验证/真机结果**：本地 TypeScript 编译和 `node --check assets/soft-keyboard.js` 通过；`node assets/hmi.test.mjs` 通过，覆盖认证-键盘矩形安全间距（上下各 `16px`）、文案/键位契约及 `1280×720` 缩放比例；`git diff --check` 通过。Go 工具不在当前环境；Browser 运行环境没有可用实例，故未执行真实点击或视觉截图验证。未连接设备、未构建发布、未部署或烧写。
- **遗留事项**：在可用 Browser 中补验登录与 bootstrap sheet 的矩形、Shift/退格/点号键行为、认证 toast 不位移和 `1280×720` 点击映射；真机发布前仍需由 Release 角色在真实 `1920×1080` 屏幕验收。

#### 底栏两端状态控件收敛

- **用户目标/决策**：仅重做底栏左右两端的视觉呈现。登录改为带用户图标的单行白色轻量按钮；自动/手动改为同尺寸状态按钮，分别使用浅绿/浅黄背景、对应状态点和深色文字；中间导航、认证、权限和模式行为保持不变。
- **实现结果**：底栏改为对称的 `220px` 两端区域，登录与模式控件统一为 `168×58px`、`18px` 圆角。登录图标由 `#operatorName` 的 CSS 伪元素呈现，因此既有账户文本、焦点和点击语义仍由原逻辑驱动；模式继续由既有 `is-auto`/`is-manual` 状态类呈现绿色或黄色。三个已版本化资源查询参数更新为 `20260807.3`。
- **对应提交**：`4da1534`。
- **验证/真机结果**：`git diff --check` 与 `node assets/hmi.test.mjs` 通过，Node 静态断言覆盖两端同尺寸、登录图标、自动/手动状态色及既有模式状态类。Browser 运行环境没有可用实例，无法执行真实点击、截图或矩形核验；Go 工具当前不可用，未运行 `ui_contract_test.go`。未连接设备、未构建、未部署或烧写。
- **遗留事项**：在可用 Browser 中补验真实 `1920×1080` 和外部 `1280×720` 缩放下的底栏矩形、登录点击及自动/手动切换；Go 可用后补跑 HMI UI 契约测试。真机发布仍须由 Release 角色获得授权后执行。

#### 认证与软键盘首帧同步打开

- **用户目标/决策**：点击底栏登录或游客访问受保护维护页时，普通登录或首次管理员认证框必须与软键盘在浏览器第一次可见绘制中同时出现，不能出现先显示框、后滑入键盘的两个可见阶段。
- **实现结果**：所有认证打开路径统一进入 `openAuthWithKeyboard`。该入口同步初始化键盘、设置认证模式与可见面板、定位普通登录或首次管理员表单的首个用户名输入、设置软键盘模式和固定状态、以即时选项打开 dock，最后使用 `focus({ preventScroll: true })` 聚焦。认证路径在 dock 可见前设置无过渡标记并同步完成认证布局；维护等普通输入仍使用原有键盘打开路径与过渡。认证、游客取消、公共导航、权限、成功后的维护跳转、demo 坐标桥和密码显示切换未改变。三个已版本化资源查询参数更新为 `20260807.4`。
- **对应提交**：`53e2dba`。
- **验证/真机结果**：TypeScript 编译、`node --check assets/soft-keyboard.js`、`node assets/hmi.test.mjs` 和 `git diff --check` 通过。Node 静态断言覆盖单一同步入口、用户名目标、即时键盘打开、无 `requestAnimationFrame`/`setTimeout` 调度及两条登录/受保护维护入口。Browser 运行环境没有可用实例，无法进行真实首帧截图、点击或矩形核验；Go 工具当前不可用，未运行 `ui_contract_test.go`。未连接设备、未构建、未部署或烧写。
- **遗留事项**：在可用 Browser 中补验底栏登录、游客维护、普通登录和 bootstrap 的首帧截图及 `1920×1080`、外部 `1280×720` 缩放矩形；Go 可用后补跑 HMI UI 契约测试。真机发布仍须由 Release 角色获得授权后执行。

#### 认证 sheet 收窄与底栏可见高度统一

- **用户目标/决策**：将普通登录 sheet 从约 `744px` 收窄至约 `500px`；首次管理员 bootstrap 复用同一宽度并按三字段自然增高。底栏左侧登录/当前账号和右侧自动/手动的可见高度必须与中间选中导航完全一致，且不得各自硬编码后漂移。
- **实现结果**：认证弹层继续由同一 `.auth-sheet` 承载登录和 bootstrap，宽度更新为 `min(500px, 100%)`，保留现有纵向留白、最大高度、密码眼睛、同步认证键盘、验证与登录结果语义。底栏抽取 `--footer-control-height: 102px`，由中间 `.nav-button`、左侧 `#operatorName` 和右侧 `.mode` 共同引用；外侧仍保持对称 `220px` 区域、端控件 `168px` 宽和 `18px` 圆角，自动绿色/手动黄色及既有点击、权限和模式逻辑未变。三个已版本化资源查询参数更新为 `20260807.5`。
- **对应提交**：`290e00b`。
- **验证/真机结果**：`node assets/hmi.test.mjs`、`node --check assets/soft-keyboard.js` 和 `git diff --check` 通过。Node 静态断言覆盖 500px 有界 sheet、登录/bootstrap 共用容器、导航与两端控件的共享高度变量，以及三项资源版本。Browser 运行环境没有可用实例，无法进行真实截图、点击或矩形核验；Go 工具当前不可用，未运行 `ui_contract_test.go`。未连接设备、未构建、未部署或烧写。
- **遗留事项**：在可用 Browser 中补验 `1920×1080` 和外部 `1280×720` 缩放下的普通登录、bootstrap 与三类底栏控件矩形；Go 可用后补跑 HMI UI 契约测试。真机发布仍须由 Release 角色获得授权后执行。

#### 发布候选：无状态认证收口与本机持久化验证

- **用户目标/决策**：在发布前收口本地无状态认证的真实行为：前端过期会话不能被迟到的输入续期，前后端字段长度遵循 Common v2 契约，并用实际 Agent 验证 SQLite 账户和 idle 配置重启后的持久化。
- **实现结果**：Common `d1073038277db0b954c021cb2cc377012ec8a78c` 定义 v2 无状态认证契约，Block `COMMON_BASELINE` 已固定该提交。随后 `0511847` 限制 username 至 128、所有密码字段至 4096 个 Unicode 字符；`5f83b61` 在每次本地活动续期前判定 `expiresAt`，过期立即回访客态；`7621280` 增加真实 `cmd/block-agent` 本机启动和独立数据库持久化回归脚本。
- **验证/真机结果**：TypeScript 编译、`node assets/hmi.test.mjs`、HMI `go test .`、Agent 离线 `go test ./...` 与 Common v2 合同校验通过。真实 Agent 持久化脚本覆盖首次管理员、错误/正确登录、idle 配置、无 Cookie、退役 `activity/logout` 路由和同库重启；发布前 Luna 门禁复核通过。未连接设备、未部署或烧写，尚无真机发布证据。
- **遗留事项**：获得 Release 授权后先执行设备只读预检和数据备份，再按发布流程部署并完成健康、版本、回滚和真实屏幕验收记录。

#### 本机 HMI HTTPS/WSS 收口（本地候选）

- **用户目标/决策**：消除本机 HMI 的明文 HTTP/WS 例外；业务页面、健康检查、API 和 WebSocket 只允许 loopback TLS，同时保留既有 `8443` 维护 HTTPS 边界。
- **实现结果**：`9657e1b` 将业务 mux 固定为 `127.0.0.1:8444`，启动必须加载证书/私钥，TLS 最低为 1.2；明文探测不返回 HTTP 兼容响应，8080/8081 不监听。`c47115b` 使 HMI 只计算同源 WSS URL。systemd、安装/回滚/验证脚本和 kiosk 改为严格 CA 校验的 `https://127.0.0.1:8444`；公开 CA 单独配置，证书/私钥仍复用既有维护路径。发布文档仅在严格检查通过、Chromium 单独缺少信任时提供受控 NSS 公开 CA 导入步骤，不允许 `-k`、`--insecure` 或忽略证书错误。
- **验证/真机结果**：Agent TLS 单元测试覆盖正确 CA、错误 CA、错误主机名、过期证书、TLS 1.1 和明文 HTTP 拒绝；HMI 静态测试覆盖 WSS；真实 Agent 持久化脚本以随机 loopback TLS 端口、严格 CA 校验验证认证和重启持久化，并检查 8080/8081 没有业务监听。未连接设备、未部署或烧写，尚无真机证据。
- **遗留事项**：Release 授权后按发布流程先做设备只读预检；真实设备必须验证 `127.0.0.1` SAN、严格健康检查、Chromium HTTPS/WSS 页面和屏幕证据。若 Chromium 需要本机 CA 信任，只能由设备管理员按文档备份并导入公开 CA。

## 维护规则

每次 Block V2 变更只在本文件**追加一条**，并同步更新顶部的功能基准、真机
版本、本地演示状态、已完成/未发布和下一步。每条只保留目标/决策、结果、提交、
验证或真机结论与遗留事项；不要写成逐命令的故障流水账。没有证据的结果必须写
“待确认”，不得补写推测。
